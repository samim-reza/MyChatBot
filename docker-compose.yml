services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mychatbot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
