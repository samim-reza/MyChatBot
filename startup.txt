# Azure Web App startup command
# Increased timeout for large ML models (sentence-transformers, torch)
gunicorn --bind=0.0.0.0 --timeout 900 --workers 1 --worker-class uvicorn.workers.UvicornWorker app:app
