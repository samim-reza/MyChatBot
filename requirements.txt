# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn==21.2.0

# Environment and HTTP
python-dotenv==1.0.0
httpx>=0.27.0

# Vector DB (lightweight — no torch, no sentence-transformers)
chromadb>=0.4.0,<0.6
posthog<3.0.0
